{
  "name": "eis",
  "description": "internet.ee",
  "scripts": {
    "start": "gulp watch & kit watch",
    "dev": "gulp watch & sudo kit watch -s dev",
    "deploy": "kit manifest && kit push assets/* && kit push images/* && kit push stylesheets/*.css && kit push javascripts/*.js && kit push layouts/*.tpl && kit push layouts/*.tpl && kit push components/*.tpl"
  },
  "devDependencies": {
    "autoprefixer": "^9.8.6",
    "cssnano": "^4.1.10",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-notify": "^3.2.0",
    "gulp-postcss": "^8.0.0",
    "gulp-sass": "^4.1.0",
    "gulp-uglify": "^3.0.2"
  },
  "dependencies": {
    "punycode": "1.3.2"
  }
}
